Video frame extraction toolkit: a C++ decode/dedup/downsample pipeline exposed over gRPC
(`cpp/`), and an Avalonia GUI client for hand-filtering the extracted frames (`dotnet/`).
Both sides share one gRPC contract: `cpp/frame_service.proto`.

@cpp/AGENTS.md
@dotnet/AGENTS.md
